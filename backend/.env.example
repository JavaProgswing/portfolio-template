# Spotify OAuth credentials (optional — only needed for /spotify/now-playing endpoint)
# Get from https://developer.spotify.com/dashboard
# Then complete OAuth flow once to get a refresh token (any spotify-oauth helper works)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
